---
// The methodology has its own page now (D1 Option B, 2026-07-07). Permanent
// redirect preserves any existing links to /how-it-works.
export const prerender = false;
return Astro.redirect('/method', 301);
---
