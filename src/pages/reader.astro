---
export const prerender = false;
// "The Reader" was the tool's old name. The tool now lives at /audit (D1
// Option B, 2026-07-07) — redirect there directly, no double hop. Existing
// links and the extension's hand-off keep working.
return Astro.redirect('/audit', 301);
---
