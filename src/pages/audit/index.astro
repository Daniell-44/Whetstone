---
export const prerender = false;
// /audit was a duplicate of the Reader (same AuditForm). The Reader is the
// homepage now, so consolidate: permanent redirect straight to / (matching
// /reader's behaviour). Shareable audits still live at /audit/[id].
return Astro.redirect('/', 301);
---
