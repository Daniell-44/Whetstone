---
export const prerender = false;

// Steelman merged into the Reader as a free lens (Decision 1, 2026-06-30). The
// standalone tool is retired; its traffic 301s to the Reader, where "Strongest
// opposing case" now appears after an audit. Kept as a redirect so saved links
// and search results still resolve.
return Astro.redirect('/reader', 301);
---
